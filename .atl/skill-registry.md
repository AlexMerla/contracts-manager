# Skill Registry — contract-manager

Generated by sdd-init. Re-run `/sdd-init` to refresh (upserts via topic_key `skill-registry`).

## User-level skills (~/.claude/skills/)

| Skill | Path | Trigger |
|---|---|---|
| typescript-style | `~/.claude/skills/typescript-style/SKILL.md` | TS/ESLint/Prettier style guide — relevant, this project's stack is Next.js + TypeScript |
| skill-creator | `~/.claude/skills/skill-creator/SKILL.md` | Creating new agent skills — not app-code relevant |
| go-testing | `~/.claude/skills/go-testing/SKILL.md` | Go/Bubbletea testing — not relevant, project has no Go code |

(`sdd-*` skills and `_shared` excluded per scan rules — they're the orchestration layer itself.)

## Project-level skills

None found. No `.claude/skills/`, `.agent/skills/`, or `skills/` directory inside the project.

## Project convention files

- **`CLAUDE.md`** (project root) — non-negotiable rules from `docs/spec.md`:
  - English code/DB identifiers; Spanish (Mexico) product-facing text (UI copy, contract content).
  - No job queue — sequential in-process background processing, retried via boolean status columns on `contracts` (spec §4.2).
  - Two separate Google OAuth flows (Auth.js login vs. Google Master Drive/Calendar) — must never share code, tokens, or sessions (spec §4.3).
  - `contract_packages` stores its own `name_snapshot`/`price_snapshot` — never join to live `packages` (spec §6.5).
  - Role permissions enforced at the data-access layer — `normal` user queries scoped by `createdById`, not just hidden in UI (spec §5).
  - `viewer_token`, never the internal `id`, is exposed in public URLs (spec §10).
  - **Workflow**: read relevant `docs/spec.md` sections → read current sprint's `specs/*/tasks.md` → implement one task at a time → stop at each task's "Done when" criterion → do not start the next sprint until its `tasks.md` exists.

## Repo state notes

- No `.git` yet — version control init is part of sprint-01 task 1.
- No `package.json` / `node_modules` / `src/` — zero application code exists yet.
