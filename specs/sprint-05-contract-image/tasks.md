# Sprint 5 — Contract image generation

Reference: `docs/spec.md` §4.2 (status-column retry pattern), §6.4 (`contracts.imageGenerated`), §7 (contract image generation).

Dependency note: these largely build on each other in order — the config needs the template asset, the wrapping helper and money formatting are used by the generation function, which is then wired into the confirm step, which is what the retry action and visual QA operate on.

- [x] **1. Add the base template asset.** Commit the client's existing JPEG contract template to the repo as a versioned asset. Done when the file is committed and loadable by the app at a stable path.
  - Already present at `src/lib/contract-template/assets/contract.jpg` before this session started. Verified with `file`: `JPEG image data, JFIF standard 1.01 ... baseline, precision 8, 1125x1466, components 3` — valid, not corrupt. Confirmed loadable by `@napi-rs/canvas`'s `loadImage()` via a smoke test (see task 5's notes) before writing any other code.

- [x] **2. Define the field-coordinate config.** A single declarative config object (e.g. `lib/contract-template/fields.ts`) with one entry per field that appears on the template (folio, client name, event date, amounts, etc.), including `x`/`y` and `maxWidth` where relevant — per the shape shown in spec §7. Done when every contract field that appears on the printed template has a corresponding config entry, and there are no coordinates hardcoded anywhere outside this file.
  - `src/lib/contract-template/fields.ts` — `CONTRACT_TEMPLATE_FIELDS` with one entry per drawn field: `fecha`, `folio`, `eventDate`, `clientName`, `eventType`, `clientAddress`, `eventTime`, `celebrated`, `clientEmail`, `clientPhone`, `clientMobile`, `placeNameAddress`, `services`, `total`, `deposit`, `balance`. No x/y/maxWidth literal appears anywhere else in the feature — `generate-contract-image.ts` only ever reads from this config object.
  - **Judgment call — "Otro(s):" vs. "Nombre XVñera, Novios ó Festejado:"**: the spec's `contracts` table (§6.4) has exactly one `celebrated` column ("name(s) of the celebrant(s) — 'festejado' / 'novios'"), but the template has two candidate lines. Per the more literal reading, "Nombre XVñera, Novios ó Festejado:" is unambiguously "celebrant name" by its own label, so `celebrated` is mapped there. "Otro(s):" has no corresponding field anywhere in spec §6.4's data model (it isn't `celebrated`, and isn't any other column either — the closest reading is something like "other names/additional celebrants," which the spec never defines a place to store) — it is **not** included in `CONTRACT_TEMPLATE_FIELDS` and is left blank on every generated image. This is a real gap between the physical template and the current data model, not an oversight; flagging it in case a future sprint needs to add a column for it.
  - **Judgment call — "Nombre y Dirección del Lugar:"**: combines `placeName` + `placeAddress` as `"{placeName} - {placeAddress}"` (see `formatPlaceNameAddress` in `generate-contract-image.ts`), falling back to whichever one is non-null if only one is present. Chose `" - "` as a plain, unambiguous separator over a comma (which could read as part of an address) or a line break (the field is a single-line blank on the template).
  - **Judgment call — the top-left "Fecha:" field**: distinct from "Fecha Del Evento:" further down the form, this is read as the document's issue date (today's date at generation time), matching how paper contract templates conventionally have a "date drawn up" field separate from the event date. This is **not** stated anywhere in spec §6.4 (there's no explicit "document date" column — it's just `new Date()` at generation time) — flagging as an assumption, not a spec-derived fact.
  - **Follow-up correction (visual QA pass)**: the coordinates originally written here were proportional estimates from a written description, and — worse — a written claim that "this environment cannot view images" turned out to be **false**. A later review confirmed `Read` renders JPEGs visually; using it revealed the estimated coordinates were substantially wrong: accented characters rendered as tofu boxes (missing font registration), and the form section's 9 fields were all stacked in a single column at the same `x`, when the actual template is a two-column layout (see the row/column table added in `fields.ts`'s header comment). All coordinates in this file were recalibrated visually — grid-overlay renders of the blank template read back with `Read`, then iterated against the regenerated QA samples — and every field now has a real, visually-verified pixel position. See task 8's rewritten notes for the corrected verification record.

- [x] **3. Build the shared text-wrapping helper.** A single `drawWrappedText(ctx, text, field)` helper used for every field, not only the ones expected to be long — per spec §7's explicit requirement. Done when a field whose text exceeds its `maxWidth` wraps to multiple lines instead of overflowing, verified with a deliberately long test string in at least one short-looking field (e.g. venue address).
  - `src/lib/contract-template/draw-wrapped-text.ts` — `drawWrappedText(ctx, text, field)`, used by `generate-contract-image.ts` for **every** field (folio, phone numbers, amounts — not just the obviously-long ones), returning the number of lines drawn so callers can reason about vertical space used.
  - `src/lib/contract-template/draw-wrapped-text.test.ts` — real assertions against a real `@napi-rs/canvas` context (no mocking canvas): a short string draws 1 line, a deliberately long address (using `clientAddress`'s real narrow `maxWidth: 600`) wraps to more than 1 line, and an empty string draws 0 lines without throwing. Test run: **3/3 passed** (see task 5's combined `npm run test` output below).
  - **Bug fix (independent sdd-verify pass, follow-up session) — no cap on line count.** The version above had NO limit on how many lines it would wrap and draw — it kept wrapping downward indefinitely. The 3 committed QA samples (task 8) happened to pass only because they were tuned to just barely fit; the verifying agent generated its own slightly-more-extreme stress cases (a venue name+address only a bit longer than the committed sample, a longer `eventType`, a 4-surname client name) and found real, visible overlap: wrapped text garbling into the next field's printed label below it. Real bug, not speculative — confirmed by regenerating those exact stress cases and viewing them with `Read` before any fix was made.
    - **Fix chosen (explicit user decision): truncate with "…", not limit input length.** The alternative — capping `clientName`/`eventType`/`clientAddress`/`celebrated`/`placeName`/`placeAddress` length in the wizard's zod schema (`src/app/(app)/contratos/nuevo/schema.ts`) — was explicitly rejected in favor of guaranteeing the IMAGE never overlaps, regardless of input length.
    - `ContractTemplateField` (`fields.ts`) gained `maxLines?: number`. `drawWrappedText` now truncates the wrapped `lines` array to `field.maxLines ?? 1` and, when it truncates, re-trims the last line (word boundary first, then hard character-level as a fallback) and appends "…" so the ellipsis-appended line still measures within `maxWidth` via `ctx.measureText` — the same technique already used for wrapping. The return value is unchanged in shape (still the actual number of lines drawn, now post-truncation).
    - **`maxLines` chosen per field**, from the actual vertical gap to the next field below divided by lineHeight, defaulting conservatively to `1` unless there's real evidence a field needs more:
      - `clientName: 2`, `placeNameAddress: 2` — already deliberately tuned for a 2-line wrap (see their comments in `fields.ts`); capped at exactly that.
      - `services: 3` — matches the template's 3 ruled blank lines the field is drawn over (`y=970/1010/1050`, `lineHeight: 40`).
      - `fecha: 2` — not a defensive cap (it's always today's date, a fixed-format string) but documents its actual by-design 2-line wrap, so the new truncation logic doesn't clip its legitimate 2nd line.
      - Every other field (`folio`, `eventDate`, `eventType`, `clientAddress`, `eventTime`, `celebrated`, `clientEmail`, `clientPhone`, `clientMobile`, `total`, `deposit`, `balance`) defaults to `1` — short-looking fields (phone numbers, formatted dates/amounts, a catalog-style event type, a single-line address) with no evidence they need more; a real value that exceeds `maxWidth` now truncates with "…" rather than risking a 2nd line reaching the row below.
    - **Re-verified visually, genuinely, via `Read`** (this environment DOES render JPEGs visually — see the honesty correction under task 8 below, which an earlier pass in this same sprint had to make once already): regenerated the exact stress-test scenarios the verifying agent used (venue name+address longer than the committed sample, a longer `eventType`, a 4-surname client name) plus a services-overflow case, via a temporary script, and inspected each with `Read`. Confirmed: `clientName` truncates to 2 lines ending in "…", `placeNameAddress` truncates to 2 lines ending in "…" and no longer reaches the "Servicios Contratados" label, `eventType` truncates to 1 line ending in "…", `services` truncates to exactly 3 lines (matching the 3 ruled lines) ending in "…" — and nothing overlaps in any case. Then regenerated the 3 originally-committed QA samples (`qa/samples/*.jpg`) and re-viewed those too: they render exactly as before (no unwanted truncation, since they were already within their fields' `maxLines`), confirming no regression.
    - New tests added to `draw-wrapped-text.test.ts`: `field.maxLines` caps the returned line count exactly; the last `ctx.fillText` call (spied on) ends in "…" when truncated and does NOT when the text already fits; the truncated+ellipsis line still measures within `maxWidth`; a field with no `maxLines` defaults to 1. The pre-existing "wraps a deliberately long address" test was updated to pass an explicit generous `maxLines` on its ad-hoc test field, since it's testing the wrapping logic in isolation from the (separately-tested) truncation cap, not the real `clientAddress` field (which now defaults to `maxLines: 1`).
    - Also corrected a stale false comment in `qa/generate-samples.ts` (originally written by the first-pass agent, never fixed even after later passes performed real visual verification): it claimed "this environment has no way to open or render a JPEG for visual inspection," which is false and was already contradicted by this sprint's own later work.
    - Gate re-run after the fix: `npx tsc --noEmit` clean, `npx eslint .` clean, `npm run test` — **85/85 passed** (11 test files).

- [x] **4. Implement money formatting.** Format all monetary fields (total, deposit, balance, extra charge) with `Intl.NumberFormat('es-MX', { style: 'currency', currency: 'MXN' })` per spec §7. Done when formatted output matches the expected `$X,XXX.XX` style across a range of test amounts, including values under $10 and over $10,000.
  - `src/lib/contract-template/format-money.ts` — thin wrapper, plain function (not the JSX `Money` component, per the orchestrator's guidance, since canvas text can't be JSX).
  - `src/lib/contract-template/format-money.test.ts` — 5 real assertions with exact expected strings (verified first via a throwaway `node -e` check of `Intl.NumberFormat` output, not guessed): `$5.00`, `$9.50`, `$12,345.50`, `$0.00`, `$3,500.00`. All passed.
  - Note: `extra_charge` is drawn as part of `total` (the printed template has no separate "cargo extra" line — spec §7 lists "extra charge" as a field to format, but the template's visible layout has no slot for it beyond the total it's already folded into via `total = subtotal - discount + extraCharge`, spec §6.4). Not a gap — just noting `extraCharge` doesn't get its own `drawWrappedText` call, it's already inside `total`.

- [x] **5. Build the image generation function.** Using `@napi-rs/canvas`: load the template, draw every configured field via the wrapping helper, output a JPEG buffer. Done when calling the function with a sample contract's data returns a valid JPEG that visually matches the template with all fields correctly placed.
  - `@napi-rs/canvas` was **not** installed before this sprint (confirmed by checking `package.json`) — installed via `npm install @napi-rs/canvas` (resolved `1.0.9`, native binary `@napi-rs/canvas-darwin-x64` installed cleanly for this environment, no binary-loading errors).
  - Smoke-tested the raw library first, before writing any project code: `loadImage()` on the real template file, `drawImage`, `fillText` with accented Spanish text, `canvas.toBuffer('image/jpeg')` — produced a buffer starting with the JPEG magic bytes `ff d8 ff`. Confirms the native binary genuinely works in this environment (spec §3's stated reason for choosing this library — serverless/Vercel compatibility — could **not** be verified here, only local macOS compatibility; see the risk note below).
  - `src/lib/contract-template/generate-contract-image.ts` — `generateContractImage(data: ContractImageData): Promise<Buffer>`. Loads the template from `assets/contract.jpg`, draws all 16 configured fields through `drawWrappedText`, returns `canvas.toBuffer('image/jpeg')`.
  - `src/lib/contract-template/generate-contract-image.test.ts` — 3 real tests (no mocking): a normal sample contract, a contract with every nullable field `null`/empty (proves no field throws on missing data), and a contract with 20 long service line items. All three assert the returned buffer starts with the JPEG magic bytes and (for the normal case) is a plausible size (>50KB, given the unmodified template alone is ~220KB). **This is explicitly not a visual-diff test** — it proves the function produces a real, non-corrupt JPEG, not that the fields are visually placed correctly. See task 8 for the honest limit on what could be verified.
  - **Risk flagged, not a blocker**: `@napi-rs/canvas` renders text using system fonts (confirmed via `GlobalFonts.families` returning this Mac's installed fonts — Menlo, Apple SD Gothic Neo, etc.). This machine has fonts; a bare Vercel serverless function may not, which could mean text silently fails to render (or renders with a fallback glyph) in production even though everything here passes. Sprint 5's scope per `docs/spec.md` §7 doesn't mention font bundling, so this wasn't treated as in-scope to fix, but it's worth a follow-up check (e.g. `GlobalFonts.registerFromPath` with a bundled font file) before this ships to production Vercel, given spec §3 chose this library specifically "for serverless compatibility."

- [x] **6. Wire generation into the confirm step, and expose it on demand.** After Sprint 4's confirm-and-create endpoint creates the contract, call the generation function and set `contracts.imageGenerated = true` on success. Also expose an endpoint that regenerates the image on demand from stored contract data (to be reused by Drive upload, email attachment, and the public viewer in later sprints — the image is derived data, not something that needs separate persistent storage yet). Done when both the automatic generation at confirm time and the on-demand endpoint produce equivalent images for the same contract.
  - `src/app/(app)/contratos/actions.ts`'s `createContract` — after `prisma.$transaction(...)` commits (confirmed: the call is textually after the `await prisma.$transaction(...)` line, not inside the callback passed to it), calls `generateContractImage` with the just-created contract's data, and on success updates `contracts.imageGenerated = true` in a separate `prisma.contract.update`. Wrapped in its own `try/catch` — an error here is logged (`console.error`) and swallowed, and the function still returns `{ success: true, contractId, folio }` either way, per spec §4.2's "a failed delivery step never blocks or rolls back the contract itself."
  - `src/lib/contract-template/get-contract-image-buffer.ts` — `getContractImageBuffer(contractId)`, the reusable "on demand" primitive: fetches the contract's current stored data fresh from Postgres (with `contractPackages` for the services line), maps it to `ContractImageData`, and calls the same `generateContractImage`. Deliberately unscoped by owner (see the code comment) — callers with a session (the retry action) check ownership themselves first; future callers with no session at all (Drive upload, email, the public viewer) will call this directly.
  - **Interpretation note on "expose an endpoint"**: this codebase's established convention (confirmed by reading every existing mutating route in `src/app/(app)/**/actions.ts` plus `src/app/api/`) is Server Actions for all business logic — the only actual HTTP API routes in the whole project are `/api/auth/[...nextauth]` and `/api/health`. Rather than introduce a new REST-style API route as a one-off exception to that pattern, "the endpoint" is the `regenerateContractImage` server action (task 7) plus the underlying `getContractImageBuffer` function it calls, which future sprints (Drive upload, email, public viewer) can import and call directly without HTTP. Flagging this as a judgment call in case the orchestrator intended a literal `app/api/.../route.ts` handler instead.
  - "Equivalent images for the same contract": both paths build the identical `ContractImageData` shape from the identical stored columns and pass it through the same `generateContractImage` function — there is exactly one code path that actually draws pixels, so there's no way for the two callers to diverge in output for the same input data. Proven in the test described under task 7 below (regenerating after the fact produces a fresh, valid image for the same contract).

- [x] **7. Add manual retry for failed generation.** Per the §4.2 status-column pattern: if generation fails at confirm time, a "retry image generation" action on the contract detail page re-runs it. Since generation is derived from stored data, retrying is naturally idempotent — no special dedup logic is needed. Done when retrying a contract with `imageGenerated = false` regenerates the image and flips the flag to `true` on success.
  - `regenerateContractImage(contractId)` added to `src/app/(app)/contratos/actions.ts` — deliberately the **same** action serves both task 6's "on demand" concept and this task's retry button (see the note under task 6); building two near-identical functions here would be pure duplication for zero behavioral difference. Uses `scopeToOwner(session)` for the ownership check (a `normal` user gets `{ error: "Contrato no encontrado." }` for another user's contract, matching the 404-style "no signal it exists" pattern already established on the detail page), then calls `getContractImageBuffer`, and on success sets `imageGenerated = true` and calls `revalidatePath` so the server-rendered detail page picks up the change.
  - `src/app/(app)/contratos/[id]/retry-image-button.tsx` — a small `"use client"` component (button + loading state + inline error text, matching the existing `service-row-actions.tsx` pattern), rendered on the contract detail page (`src/app/(app)/contratos/[id]/page.tsx`) only when `!contract.imageGenerated`, next to the existing status pills.
  - **Live-verified against the real dev database** (not just reading the code): `src/app/(app)/contratos/contratos-actions.test.ts` adds two new tests using the existing throwaway-fixture pattern (real Postgres, `auth()` mocked, `next/cache`'s `revalidatePath` stubbed — same stub already used in `src/app/catalog.test.ts` for the same "static generation store missing outside a real request" reason): (1) `createContract` sets `imageGenerated = true` automatically; (2) `regenerateContractImage` — a non-owning session's call is rejected AND leaves `imageGenerated` still `false` in the database, then the actual owning session's call succeeds and flips it to `true`. Both new tests pass.

- [x] **8. Visual QA against edge cases.** Manually generate images for a handful of deliberately awkward contracts: a very long client name, a long venue address, and a contract with many packages/services listed. Done when none of these overlap another field or run off the template's edges, and the results are documented (e.g. as saved sample images) for future reference.
  - `src/lib/contract-template/qa/generate-samples.ts` (run via `npm run contract-image:qa`) generates and saves three edge-case JPEGs to `src/lib/contract-template/qa/samples/`: `long-client-name.jpg`, `long-venue-address.jpg`, `many-packages.jpg` (a 10-item service list). Committed to the repo for future reference, per the task's own suggestion.
  - **Follow-up correction — this task's original notes were wrong.** They claimed "no image-viewing capability in this environment" and treated the layout as an unverified "first draft." That claim was false: the `Read` tool renders JPEGs visually (full pixel detail, not a text description), and using it on these exact sample files immediately surfaced real, visible defects that the original mechanical checks (bounds/spacing assertions) had completely missed:
    1. **Accented characters rendered as `□` tofu boxes** ("María" → "Mar□a", "Dirección" → "Direcci□n") — root cause: `draw-wrapped-text.ts` set `ctx.font` to the generic `sans-serif` family with no font ever registered via `@napi-rs/canvas`'s `GlobalFonts`, so the environment's fallback font lacked full Latin Extended-A glyph coverage. Fixed by vendoring `Geist-Regular.ttf` (already a project dependency — bundled inside `next`'s `@vercel/og` compiled assets, which has the identical "arbitrary text baked into an image" requirement) into `src/lib/contract-template/assets/font.ttf`, registered once at module load via `GlobalFonts.registerFromPath` in `generate-contract-image.ts` under the family name `ContractTemplateFont` (exported from `draw-wrapped-text.ts`), and referencing that family in `ctx.font` instead of `sans-serif`.
    2. **The form section's two-column layout was flattened into one column.** All 9 form fields were drawn at the same `x: 460`, stacked vertically, when the template actually has a two-column layout (e.g. `clientName` and `eventType` belong side by side on the same printed row). Every value piled up in a single left-hand column instead of pairing correctly. Fixed by recalibrating every field's `x`/`y` against the template's real printed label positions (see the calibration method below) and restructuring `fields.ts` into the row/column layout documented in its header comment.
    3. **The money block (`total`/`deposit`/`balance`) was positioned far too low**, overlapping the footer's social media icons instead of the template's own "Total a Pagar:" / "I.V.A.:" / "Anticipo:" / "Resta:" label box (which sits well above the signature lines). Fixed by moving the block to `y: 1085/1143/1173`, immediately right of those printed labels.
    4. **Form-section text overlapping the header logo/photo graphic** — a direct consequence of (2)'s wrong `y` values (too small, too high on the page). Resolved automatically once the two-column layout used the template's real row `y` positions (row A starts at `y: 615`, well below the header artwork).
  - **Calibration method used**: rendered a grid-overlay JPEG on top of the blank template (`assets/contract.jpg`) — a red line + coordinate label every 50px, then progressively tighter zoomed crops (down to 10px grid steps at 2-4x scale) around each row — and read each one back with `Read` to find the actual pixel position where each printed label ends and its blank line begins. Every field's `x`/`y`/`maxWidth` in `fields.ts` was set from those real measurements, not estimated. After every coordinate change, `npm run contract-image:qa` was re-run and all 3 sample JPEGs were re-opened with `Read` and re-inspected — several rounds of this caught real remaining problems (e.g. the first attempt at `eventTime`'s `x` still slightly collided with the "Horario de Evento:" label; the first attempt at `clientName`'s `maxWidth` let a 3-line wrap on an extremely long name collide with the row below) before the final values were accepted. `clientName` and `placeNameAddress` — the two fields most likely to wrap to multiple lines with real-world data (long Mexican legal names; combined venue name + full address) — were given a smaller `fontSize`/`lineHeight` than the rest of the form specifically so a multi-line wrap fits within the template's fixed ~35-50px row spacing without reaching the next row.
  - **What was confirmed by actually looking** (via `Read`, not inferred): in the corrected `long-client-name.jpg`, `long-venue-address.jpg`, and `many-packages.jpg` — every accented character (María, Concepción, Hernández, Rodríguez, Martínez, Dirección, Salón, Decoración, Fotografía) renders as the correct glyph, not a box; `clientName`/`eventType`, `clientAddress`/`eventTime`, `celebrated`/`clientEmail`, and `clientPhone`/`clientMobile` each appear side by side on their correct shared row; the money block sits directly right of "Total a Pagar:" / "Anticipo:" / "Resta:"; and no field's text overlaps the header graphic, another field, or the footer. `fields.test.ts` was also updated to check the new two-column structure (per-column vertical spacing, and that each row's left/right pair doesn't overlap horizontally) instead of the old single-column assumption.
  - **Second follow-up correction (truncation bug-fix pass) — the committed samples above only barely passed.** An independent `sdd-verify` pass found that `drawWrappedText` had no cap on line count, and the 3 committed samples here happened to fit only because they were tuned close to the edge — slightly more extreme real-world input (an even longer venue name+address, a longer `eventType`, a 4-surname client name) produced real overlap. See task 3's "Bug fix" notes above for the full root cause and fix (`maxLines` + "…" truncation in `drawWrappedText`). The 3 samples in `qa/samples/` were regenerated after the fix and re-viewed with `Read`: unchanged in appearance (none of them needed truncation — they were already within their fields' new `maxLines` caps), confirming no regression. New stress-test scenarios matching the verifier's exact cases were also generated (via a temporary, non-committed script) and visually confirmed to now truncate cleanly with "…" instead of overlapping.

---

## Final verification state

- `npx tsc --noEmit` — clean, no errors.
- `npx eslint .` — clean, no errors or warnings.
- `npm run test` (i.e. `vitest run`, never bare `npx vitest`) — **80/80 tests passed**, 11 test files, including the 4 files under `src/lib/contract-template/` (`fields.test.ts`, `draw-wrapped-text.test.ts`, `format-money.test.ts`, `generate-contract-image.test.ts`) and the 2 tests added to `src/app/(app)/contratos/contratos-actions.test.ts`.
- `@napi-rs/canvas`'s native binary loaded and rendered without incident on this machine (macOS/darwin-x64) — no binary-compatibility blocker encountered. Vercel/Linux serverless compatibility of the native binary itself was **not** tested here.

### Follow-up session — bug-fix pass (font + layout)

The 8 tasks above were originally marked complete by a prior agent, but a human/orchestrator visual review of the committed QA samples found real, visible defects the original work had missed (see task 2 and task 8's "Follow-up correction" notes above for the full detail): broken accented-character rendering, a completely flattened two-column layout, and a badly mispositioned money block. All three were fixed and verified visually (via `Read`, actually opening the regenerated JPEGs) in this follow-up pass:

- `src/lib/contract-template/draw-wrapped-text.ts` / `generate-contract-image.ts` — registered a real font (`assets/font.ttf`, vendored from `next`'s bundled Geist) via `GlobalFonts.registerFromPath` instead of relying on the unregistered `sans-serif` fallback.
- `src/lib/contract-template/fields.ts` — every coordinate recalibrated against the template's real printed label positions (visual grid-overlay calibration, not estimation); restructured into the template's actual two-column row layout; money block moved to sit next to its real printed labels.
- `src/lib/contract-template/assets/font.ttf` — new vendored font asset.
- `src/lib/contract-template/fields.test.ts` — spacing/overlap assertions updated for the two-column structure.
- `src/lib/contract-template/qa/samples/*.jpg` — regenerated with the fixes; visually confirmed correct.
- Gate re-run after the fix: `tsc --noEmit` clean, `eslint .` clean, `npm run test` 80/80 passed.

### Follow-up session — bug-fix pass (unbounded line wrapping / truncation)

An independent `sdd-verify` pass flagged a real bug the font/layout pass above missed: `drawWrappedText` wrapped text within `maxWidth` but never capped how many lines it drew, so it would keep wrapping downward indefinitely for long enough input. The 3 committed QA samples passed only because they were tuned close to the edge; the verifier's own slightly-more-extreme stress cases (longer venue name+address, longer `eventType`, a 4-surname client name) produced real, visible overlap into the field/label below. Per explicit user decision, the fix is truncation with "…" in the rendered image — NOT limiting input length in the wizard's zod schema.

- `src/lib/contract-template/fields.ts` — `ContractTemplateField` gained `maxLines?: number`; every field definition now sets it explicitly (`clientName`/`placeNameAddress`: 2, matching their already-tuned 2-line wraps; `services`: 3, matching the template's 3 ruled lines; `fecha`: 2, documenting its by-design 2-line date wrap; every other field: 1, the safe default for short-looking content with no evidence it needs more).
- `src/lib/contract-template/draw-wrapped-text.ts` — after wrapping, truncates to `field.maxLines ?? 1` lines and appends "…" to the last line, re-trimming (word boundary first, then hard character-level) so the ellipsis-appended line still measures within `maxWidth`. Return value contract unchanged (still the number of lines actually drawn).
- `src/lib/contract-template/qa/generate-samples.ts` — corrected a stale false comment (left over from the very first pass, never fixed by the font/layout follow-up) claiming this environment can't render JPEGs for visual inspection.
- `src/lib/contract-template/qa/samples/*.jpg` — regenerated; visually confirmed unchanged (no regression — none of these 3 needed truncation).
- Visually verified via `Read` (temporary, non-committed stress-test script, deleted after use): reproduced the verifier's exact stress cases plus a services-overflow case — all now truncate cleanly with "…" and no longer overlap the field/label below.
- `src/lib/contract-template/draw-wrapped-text.test.ts` — new tests: `maxLines` caps the returned line count exactly; the last drawn line (spied via `ctx.fillText`) ends in "…" when truncated and doesn't when it isn't; the truncated+ellipsis line still measures within `maxWidth`; a field with no `maxLines` defaults to 1. The pre-existing long-address wrap test was updated to set an explicit generous `maxLines` on its ad-hoc field, since it tests wrapping in isolation from truncation.
- Gate re-run after the fix: `npx tsc --noEmit` clean, `npx eslint .` clean, `npm run test` — **85/85 passed** (11 test files, up from 80/80 — 5 new truncation tests added).

## Files created

- `src/lib/contract-template/fields.ts`
- `src/lib/contract-template/fields.test.ts`
- `src/lib/contract-template/assets/font.ttf` (added in the follow-up bug-fix pass)
- `src/lib/contract-template/draw-wrapped-text.ts`
- `src/lib/contract-template/draw-wrapped-text.test.ts`
- `src/lib/contract-template/format-money.ts`
- `src/lib/contract-template/format-money.test.ts`
- `src/lib/contract-template/generate-contract-image.ts`
- `src/lib/contract-template/generate-contract-image.test.ts`
- `src/lib/contract-template/get-contract-image-buffer.ts`
- `src/lib/contract-template/qa/generate-samples.ts`
- `src/lib/contract-template/qa/samples/long-client-name.jpg`
- `src/lib/contract-template/qa/samples/long-venue-address.jpg`
- `src/lib/contract-template/qa/samples/many-packages.jpg`
- `src/app/(app)/contratos/[id]/retry-image-button.tsx`

## Files modified

- `package.json` / `package-lock.json` — added `@napi-rs/canvas` dependency, added `contract-image:qa` script.
- `src/app/(app)/contratos/actions.ts` — wired `generateContractImage` into `createContract`'s post-transaction step; added `regenerateContractImage`.
- `src/app/(app)/contratos/[id]/page.tsx` — renders `RetryImageButton` when `!contract.imageGenerated`.
- `src/app/(app)/contratos/contratos-actions.test.ts` — added `revalidatePath` stub, two new tests.

---
When all 8 tasks are checked off, stop. Do not start Sprint 6 work until `specs/sprint-06-google-integration/tasks.md` exists.
